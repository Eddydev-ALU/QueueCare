{
  "name": "queuecare-api-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "newman run QueueCare.postman_collection.json -e QueueCare.postman_environment.json --reporters cli,json --reporter-json-export results.json",
    "test:html": "newman run QueueCare.postman_collection.json -e QueueCare.postman_environment.json --reporters cli,htmlextra --reporter-htmlextra-export report.html"
  },
  "devDependencies": {
    "newman": "^6.1.3",
    "newman-reporter-htmlextra": "^1.23.1"
  }
}
